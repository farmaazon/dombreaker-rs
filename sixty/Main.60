import { Board } from "./Board.60";
import { GameModel, DominoModel } from "./Models.60";

export Main := Window {
    property<GameModel> game;
    // property<[DominoModel]> dominoes;
    callback domino-clicked <=> board.domino-clicked;

    VerticalLayout {
        Text {
            text: "Score: " + root.game.score;
        }

        board := Board {
            game: root.game;
        }
    }
}