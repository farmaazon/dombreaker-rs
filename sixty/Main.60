import { Board } from "./Board.60";
import { GameModel, DominoModel } from "./Models.60";

export { GameModel }

export Main := Window {
    VerticalLayout {
        Text {
            text: "Score: " + GameModel.score;
        }

        board := Board {}
    }
}